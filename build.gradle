apply plugin: 'java'

buildscript {
    repositories {
        jcenter()
    }
}

allprojects {
    repositories {
        jcenter()
    }
}

dependencies {
    compile 'com.google.code.gson:gson:2.8.0'
}
